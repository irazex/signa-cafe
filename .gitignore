# OS
.DS_Store
.thumbnail

# Local dev
node_modules/
dist/

# Secrets — never commit
/.htpasswd
.htpasswd
/.env
.env
*.pem
*.key

# Логины админки (bcrypt) и счётчик неудачных попыток — как .htpasswd, в репо не место
/.admin-auth.php
/.admin-throttle.json

# Backup files
*.bak
.openai_key

# Runtime analytics logs written by track.php (never commit, never deploy)
analytics/
