# Signa Cafe - Tasks

## 🔒 Безопасность - аудит 2026-08-28

Прогон скилла [vibe-audit](https://github.com/haraldalder-vibemogger/vibe-audit)
по всем репозиториям `irazex/*` + ручная проверка каждой находки.

- [ ] 🟡 `key.php` не проверяет ничего сам - его единственная защита это правило
      `<Files "key.php">` в `.htaccess`. Пропадёт `.htaccess` при FTP-деплое или сменится
      `AllowOverride` на хостинге - и `GET /key.php` отдаёт ключ OpenAI кому угодно.
      Репозиторий ПУБЛИЧНЫЙ, механика описана всем желающим.
      Продублировать проверку внутри самого `key.php` (`$_SERVER['PHP_AUTH_USER']`, иначе 401) - 3 строки
- [ ] 🟡 Проверить снаружи: `curl -s -o /dev/null -w '%{http_code}' https://signa.cafe/key.php`
      должно быть `401`

Отдача ключа в браузер сама по себе - осознанное решение: хостинг в RU-регионе,
OpenAI его геоблокирует, поэтому браузер админа ходит в OpenAI напрямую.
Претензия не к этому, а к тому, что защита держится на одном файле конфига.

Ложное: CORS `*` в `track.php:23` - трекер, `*` по назначению.

Полный отчёт и разбор ложных срабатываний по всем проектам - в хабе:
`CLAUD MAIN AGENT/reference/security-audit-2026-08-28.md` и
`security-fix-plan-2026-08-28.md`.

---

## ⬜ Backlog

## 🟡 В работе

## ✅ Сделано (последнее)

## 📝 Открытые вопросы к пользователю
