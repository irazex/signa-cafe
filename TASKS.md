# Signa Cafe — TASKS

> Точка входа без контекста - [HANDOVER.md](HANDOVER.md) (состояние прода, что крутится
> само, деньги, доступы). Здесь - только незавершённая работа и что закрыто последним.

## 🟡 В работе

- [~] **Stories: исправить последний пост о Solo Chicken-brocoli** - проверить блюдо по Syrve и живую статью, убрать выдуманную историю и навязанный термин «белая пицца», заменить ошибочную обложку с сырниками реальным фото блюда, закрыть ошибочный фото-фолбэк в генераторе. Проверить EN/RU/ID и production после самостоятельного деплоя. 27.09.2026.

- [~] **Ответить на новые неотвеченные отзывы Google Business Profile** (27.09.2026).
      Проверить кабинет Signa Cafe, опубликовать персональные ответы на языке гостей
      и повторно убедиться, что открытых отзывов не осталось. Попытка 27.09:
      веб-профиль автоматизации и локальный Chrome перенаправили на вход Google.
      Отзывы не прочитаны, ответов не опубликовано. TinyFish удалён по просьбе
      владельца 27.09. Продолжить через другой авторизованный доступ к GBP.

- [ ] **10б. Google Business Profile через API** - доступ к Business Profile API
      Google выдаёт по заявке, мгновенно его не получить. До одобрения работает
      ручной путь: готовый пост приходит в Telegram вторым сообщением, а на отзывы
      отвечает автопилот через DOM веб-кабинета.
- [ ] **Пуш с VPS в GitHub** - крон коммитит локально и деплоит по FTP, но `git push`
      падает без токена. Одна команда от владельца включит архивирование в GitHub.
      Пока не включено, коммит на VPS - единственная копия свежего поста до
      ближайшего ручного `git pull` на Маке.

## ⬜ Backlog


- [ ] Рассмотреть server-side рендер для 4 основных страниц (сейчас закрыты `<noscript>`-фолбэком)
- [ ] Убрать Babel-standalone (~600 КБ в браузере) - это главный тормоз Core Web Vitals
- [ ] Дособрать переводы UI для Brand/Menu/Signature/Experience/Order (там до сих пор EN)
- [ ] Убрать мёртвый фолбэк ключа в `tools/story-gen.mjs` (`~/.razex-creds/openai.txt`,
      `.openai_key`) - файлов нет ни на Маке, ни на VPS, ключ живёт в `.env`

## 📝 Открытые вопросы к пользователю

- **Вход в Google Business Profile**: для ответов на новые отзывы нужен доступ
  к авторизованному кабинету Signa Cafe через другой браузер или API.
  Пароль в чат не присылать.

## ✅ Сделано (последнее)

- [x] **Сверка прода после переезда на SSD** - 25.09.2026: каноническая папка
      `Projects/Active/Signa Cafe Website` стояла на `804b816` (07.09) и догнана
      fast-forward до `b8e13b5` = `origin/main`. 10 коммитов с 14.09 сделаны в
      старой папке `_Projects/signa-cafe-repo`, 2 коммита крона подтянуты туда с
      VPS, пушились все оттуда же. Прод не отстаёт: `index/about/menu/visit.html`,
      `data/stories.json` и оба ассета поста mille-feuille побайтово равны
      `origin/main`; `/stories`, `/stories/ru`, `/stories/id` и страница поста
      отдают 200 и несут GTM из `lib/stories.php`. Со старой канонической копии
      ничего не заливалось, заливка не понадобилась. `src/admin.jsx` и
      `data/story-costs.json` снаружи закрыты 403, без входа в админку их не
      сравнить. VPS-клон крона на `b8e13b5`, своих коммитов нет, изменён только
      `data/story-costs.json`: крон 24.09 09:05 WITA не поставил в очередь новый
      пост - quality gate дважды отклонил Celery Goodness. Последний пост в
      очереди - 24.09, следующий прогон крона - 01.10.

- [x] **GA4 подключён через GTM** - 23.09.2026: в контейнере `GTM-WN7NK7GD`
      (аккаунт `Signa cafe`, владелец `signacafe@gmail.com`) заведён тег
      **GA4 - Signa Cafe** типа Google Tag с `G-1D77CPGEML` на триггере
      Initialization - All Pages; опубликована версия 2 «Add "GA4 - Signa Cafe"
      Google tag». Ресурс GA4 «Signa.cafe» существовал с самого начала - это тот
      самый `G-1D77CPGEML` из старой шапки CLAUDE.md, до сегодня он просто ничего
      не получал. Read-back прода: на `/`, `/menu.html` и `/stories/ru/<slug>`
      уходит `/g/collect?tid=G-1D77CPGEML` с событиями `page_view` и `scroll`.
      Realtime в GA4 подтвердил приём: 2 активных пользователя, `page_view` x4,
      `scroll` x4, `session_start` x2, и в разрезе страниц видны и React-страницы,
      и серверная RU-статья Stories - то есть считается весь сайт, а не только главная.
      Механика доступа: контейнер виден только из Arc (там залогинен
      `signacafe@gmail.com`), автоматизация - AppleScript + JS по конкретному
      номеру вкладки; у `i@razex.ru` доступа к этому контейнеру нет.

- [x] **Google Tag Manager на публичных страницах** - 23.09.2026, `9025e65`:
      контейнер `GTM-WN7NK7GD` стоит в `<head>` и `<noscript>`-фолбэком сразу
      после `<body>` на index/menu/about/visit; серверные Stories берут его из
      `st_gtm_head()` / `st_gtm_body()` в `lib/stories.php` - id прописан один раз,
      а не копией на страницу. Админка и PWA-обёртки столов намеренно без счётчика:
      первая приватная, вторые редиректят раньше, чем тег успеет загрузиться.
      Read-back прода: 9 URL (три языка Stories тоже) отдают 200 с обоими блоками,
      в браузере `gtm.js` загружается и контейнер инициализируется.

- [x] **Stories: массовая нативная вычитка RU/ID** - 19.09.2026, `90eee22`:
      все 7 предыдущих RU-версий и все 7 существующих ID-версий независимо
      пересозданы по `sourceFacts`, а русский дополнительно вычитан вручную.
      В генераторе закреплены разные композиции EN/RU/ID, RU-антиштампы и запрет
      английского menu-copy в ID. Локально 8/8 постов без предупреждений;
      production read-back: 13 локализованных URL, индексы RU/ID, RSS, sitemap и
      llms.txt отдают 200. Защищённый расход месяца - $7.37/$10; лимиты крона
      остались $3/день, $10/месяц и $5/прогон.

- [x] **Ручной запуск автопилота отзывов Google пропущен** - 19.09.2026:
      кабинет GBP во встроенной web-automation сессии открылся на форме входа
      Google (email/phone). Пароли/капчи не вводились, отзывы не сканировались,
      ответы не публиковались. Требуется залогиненный GBP-профиль в браузере
      автопилота.

- [x] **Stories: нативная transcreation вместо перевода** - EN создаётся как
      оригинал и атомарное `sourceFacts`-досье; RU/ID пишутся независимо по фактам,
      без английских абзацев и общей структуры. Quality gate не публикует плохую
      версию. RU `mille-feuille-nutella-nusa-dua` переписан и вычитан. Расход
      исправления $0.27, период после 06.09 - $2.02/$10. Live read-back: новый
      текст, основные гео, RSS, sitemap и llms.txt - 200; IndexNow - 200. VPS на
      `7e50a64`, cron активен. Код `987a62d`, deploy `7e50a64`, 19.09.2026.

- [x] **Автопилот отзывов отработал первый настоящий прогон** - 07.09.2026 10:03 WITA
      Routines-крон `google-reviews-autopilot` сам нашёл 5 неотвеченных отзывов Signa
      (первые 6 страниц кабинета, 300 отзывов, 3 заведения), опубликовал 5, read-back
      теми же страницами - неотвеченных 0. Каждая публикация подтверждена сетевым
      `batchexecute?rpcids=XkzR1` -> 200. Лог: `ХАБ/venues/signa-cafe/google-reviews-log.md`,
      коммит в хабе `8f862fbd`. Следующий запуск чт 10.09 10:02 WITA.
      ⚠ Негатив дня (Luna9, 2★): второй раз подряд неполная доставка у одного гостя -
      картошку не положили. Это упаковка, а не кухня; в зал ушла рекомендация сверять
      пакет с чеком перед выдачей курьеру.

- [x] **`.env` для ключа OpenAI - закрыто** (проверено 07.09.2026). Файл лежит на обеих
      машинах: локально и `/home/razex/signa-cafe-repo/.env` на VPS (оба от 05.09 14:37,
      180 байт, `OPENAI_API_KEY`). `~/.razex-creds/openai.txt` больше нет ни на Маке, ни на
      VPS - четверговый крон уже живёт на `.env`. Фолбэки в `story-gen.mjs` стали мёртвым
      кодом, вынесены в backlog.

- [x] **Раздел Stories перенесён на IJEN SPA** - 05-06.09.2026, все 5 постов live, крон
      `0 11 * * 4` включён (клон `/home/razex/ijen-spa-repo`, deploy key с push - в отличие
      от Signa). Что сделано иначе: на VPS нет PHP, поэтому пре-рендер в статику вместо
      рендера на лету; 5 языков (+ZH/KO); недельный кап `IJEN_WEEKLY_CAP=$4` вместо
      месячного; ключ OpenAI общий с Signa, реестры расходов раздельные. Главное отличие
      конвейера - формула владельца «сначала EN, потом художественный перевод на каждый
      язык» отдельным вызовом; редакторский проход убран. Первый пост от крона - 17.09.
      Детали: `ХАБ/projects/ijen-spa.md`, секция Stories.

- [x] **Скилл `google-reviews-reply` + автопилот в Routines** - `~/.claude/skills/google-reviews-reply/` (SKILL.md, reference/browser-scripts.md, reference/reply-style.md, scripts: parse_custom / gen_replies / split_map / analyze). Крон `google-reviews-autopilot` в планировщике приложения: пн и чт 10:00, первые 6 страниц кабинета, только Signa, лог в `ХАБ/venues/signa-cafe/google-reviews-log.md`. Требует залогиненный кабинет GBP во встроенном браузере и открытое приложение. Commit 52ccce9, 2026-09-06.

- [x] **11. Google Search Console + Bing Webmaster**. Google 05.09.2026: сайт - DNS-подтверждённый
      Domain property, `sitemap.xml` (21 URL) и `feed.xml` отправлены, индексация запрошена для
      6 EN-адресов. Bing 06.09.2026: владелец вошёл через Google и импортировал сайт из GSC
      (верификация не нужна), агент отправил `sitemap.xml` (Processing) и 21 URL через
      URL Submission (квота 10 000/день). `tools/verify-site.mjs` не понадобился. Кода нет.

- [x] **Анализ отзывов Google** - 1 494 отзыва сняты из GBP, разбор по темам, годам и
      языкам: `ХАБ/venues/signa-cafe/google-reviews-analysis-2026-09.md` (сырые данные рядом).
      05.09.2026.
- [x] **Ответы на все неотвеченные отзывы в Google Business Profile** - 362 отзыва
      Signa с 2019 по 09.2026 закрыты за один вечер через веб-кабинет во встроенном
      браузере (API ждёт заявки). Read-back по всем 164 страницам: 1 494 отзыва Signa,
      неотвеченных 0. Ответы на языке отзыва (EN/RU/ID), от менеджера, с фактами сайта и
      гео Nusa Dua / Bukit / Ungasan; 274 написаны руками под текст, 88 - пул по рейтингу.
      Механика, селекторы и грабли (оптимистичный UI, буфер performance 250) -
      `ХАБ/projects/signa-cafe-site.md`. 05.09.2026.
- [x] **Stories: текст на «бумажном полотне»** - статья и индекс на подложке Paper с
      рамкой и жёсткой тенью (та же поверхность, что у scribble-note), 12px поля на
      телефоне, `?v=20260905b`. `ac97410` + `e3a4e32` (полоса под шапкой: `main{display:flow-root}`), 05.09.2026.

- [x] **Месячный потолок $10 на генерацию постов** - `IJEN`/`SIGNA_MONTHLY_CAP` (10),
      проверка до запроса по реестру, отсчёт с `SIGNA_CAP_SINCE` 2026-09-06, баннер caps/spent.
      `840e9b0`, 05.09.2026. Hard limit в кабинете OpenAI - за владельцем.
- [x] **Крон включён обратно** - VPS на `840e9b0`, строка `0 9 * * 4` активна (read-back
      `crontab -l` + баннер генератора). Следующий запуск чт 10.09 09:00 WITA; без пополнения
      баланса упадёт безвредно («no credits»). 05.09.2026.
- [x] **Инструкция + скилл для IJEN SPA** - `~/.claude/skills/weekly-seo-stories/reference/ijen-spa-handoff.md`
      (349 строк: что копировать, что иначе, 12 предохранителей по деньгам, чек-лист, порядок работ,
      что нужно от владельца) + SKILL.md раздел 3 и грабли обновлены по факту $96.69. 05.09.2026.

- [x] **Kampial вычищен из title / seoTitle / description / keywords / tags всех постов**
      (05.09.2026, отмашка владельца, $0 - без модели). `tools/fix-geo-text.mjs` доработан:
      регистр (ключевые слова строчные - старый тест их пропускал), адрес охраняется
      по предложению, а не по абзацу, теги на уровне поста, согласование «кафе расположено».
      Два обрубка поправлены руками. На бою: в title/description ноль, на странице ноль
      упоминаний вне `Jl. Raya Kampial`. IndexNow пингован.

- [x] **Форма входа в админку вместо Basic Auth** (задача от 05.09.2026).
      Браузерный диалог Basic Auth не показывался ни в Arc, ни во встроенном
      браузере Claude: обе программы рисовали апачевскую страницу «401
      Unauthorized», а ввести пароль было негде. Обход `https://user:pass@…`
      пускал документ, но дальше Chromium резал собственные запросы страницы,
      и админка падала на «Could not load content.json» (файл публичный,
      анонимный curl получал 200 - то есть дело было не в сервере).
      Сделано: `lib/auth.php` (PHP-сессия, кука `signa_admin` на 30 дней, CSRF,
      троттлинг 8 попыток / 15 мин на IP, bcrypt-хэши в gitignored
      `/.admin-auth.php`), `admin.php` вместо `admin.html` с формой входа в
      фирменном стиле, выход по `?logout=1` из сайдбара. Basic Auth снят
      полностью. `key.php` / `translate.php` / `analytics.php` проверяют сессию
      сами; `src/admin.jsx` и два JSON с расходами закрыты в `.htaccess`
      наглухо и выдаются через `admin.php?asset=jsx|costs|pricing`.
      Из `admin.jsx` удалён мёртвый `LoginGate` - он держал sha-256 пароля
      прямо в файле. Логины `razex` и `admin` (один пароль), чтобы
      расхождение доков и `.htpasswd` больше никого не путало.
      Проверено на бою: аноним на `/admin` видит только форму (разметки
      приложения в ответе нет), с сессией - приложение; `key.php` анониму
      отдаёт `AUTH_REQUIRED`, ключ не утекает; `src/admin.jsx`,
      `story-costs.json`, `model-pricing.json`, `.admin-auth.php` - 403,
      `lib/` - 404. Старый `admin.html` удалён с хоста.

- [x] **Модель генератора зафиксирована: `gpt-5.5`** (решение владельца 05.09.2026
      по замеренным цифрам). $0.40 за пост EN+RU+ID, ~$21 за 52 поста в год.
      Более дешёвые варианты (5.4 за $10/год, 4o-mini за $0.44/год) отвергнуты
      не по цене, а по качеству: разница в несколько долларов в год не стоит
      плохого русского. Pro закрыт флагом `--allow-pro`.

- [x] **Утечка «брошенное задание продолжает биллиться» закрыта** (05.09.2026 вечером).
      Cancel + учёт usage на любом выходе по ошибке, `Idempotency-Key` на создании.
      Реестр сведён с кабинетом за полные сутки: **$96.69 = $96.69**, 26 pro-вызовов,
      остальные 72 запроса дня прошли по incentivized-tier за $0. Крон на VPS
      **отключён** по требованию владельца (строка закомментирована, бэкап рядом);
      включать обратно осознанно. Деплой стори-раздела выполнен: слаги без Kampial,
      старые адреса 301, реестр и тарифная карта закрыты Basic Auth.

- [x] **Инцидент: $96.69 за день на gpt-5.5-pro** (05.09.2026, коммит `f8acb2c`;
      цифра уточнена по выгрузке за полные сутки, первая оценка была $57.22 / 17 вызовов).
      26 вызовов, весь счёт аккаунта за день, баланс ушёл в минус и API встал сам. Причина двойная: pro в 6 раз дороже
      за токен И пишет в 5 раз больше токенов (половина - reasoning, в статью не идёт),
      итого вызов дороже в ~30 раз. Ни потолка, ни учёта в тот момент не было.
      Разбор: `ХАБ/context/incident-signa-stories-pro-model-overspend.md`.
      Поставлено: запрет pro без `--allow-pro`, суточный потолок $3 (`SIGNA_DAILY_CAP`,
      проверяется ДО запроса), потолок на прогон `--budget`, `max_output_tokens`
      12000/12000/8000 вместо 40000/32000/24000, сверка реестра с кабинетом
      (`--reconcile`), тарифная карта проверена по developers.openai.com.

- [x] **Учёт расходов и вкладка Costs в админке** (задача от 05.09.2026).
      `tools/cost-ledger.mjs` пишет строку на каждый вызов модели в
      `data/story-costs.json`: токены, секунды, стадия, пост. Токены - факт от API,
      деньги - токены x тарифная карта в `data/model-pricing.json`, редактируемая
      прямо во вкладке (меняешь ставку - вся страница пересчитывается).
      В админке: расход, стоимость поста, разбивка по вызовам, таблица по месяцам,
      блок счёта (ставка за пост -> сумма и маржа) и выгрузка CSV.
      ⚠ Тарифная карта помечена неподтверждённой - сверить с прайсом OpenAI до счёта.

- [x] **География перевешена на Нуса Дуа / Букит / Унгасан** (задача от 05.09.2026).
      `GEO_PRIMARY` (обязательны все три) против `GEO_SECONDARY` (Кампьял, Беноа,
      Джимбаран - не больше 3 упоминаний на пост и никогда в заголовке, описании
      или слаге). Валидатор ловит нарушения, режим `--fix-geo` переписывает
      только заголовок/описание/лид уже готовых постов, не трогая текст.
      Слаги с `kampial` переименованы, старые адреса живут через `aliases` + 301.

- [x] **Переименование постов без потери URL** - `tools/rename-story.mjs`.
      Старый слаг уезжает в `aliases`, `st_find_alias()` в `lib/stories.php`
      отдаёт 301 на новый адрес. Переименования - данные в stories.json,
      а не растущий список правил в `.htaccess`.

- [x] **Кредиты OpenAI пополнены** - индонезийская версия дописана всем 5 постам.

- [x] **Скилл `weekly-seo-stories`** - `~/.claude/skills/weekly-seo-stories/`.
      Описывает всю схему: замер видимости для ботов, выбор режима рендера,
      два прохода генерации, валидатор, пул тем, крон-конвейер, раздача
      (RSS/sitemap/llms.txt/IndexNow), JPEG для соцсетей, уведомление и GBP.
      Плюс `reference/porting-checklist.md` и `reference/ijen-spa-plan.md`.

- [x] **Каталог блюд из Syrve** (`tools/syrve-menu.mjs`) - через RPC-адаптер (8300),
      джойн `online-content-map` + `menu/5352/items` + `nomenclature/products`.
      **194 блюда** с фото, описанием и ценой против 12 в `content.json` -
      это ~3,7 года еженедельных постов. Бары и бутылки отфильтрованы, остаётся 169.
- [x] **Генератор на `gpt-5.5-pro` через Responses API.** `chat/completions` для pro
      отвечает «This is not a chat model». Запрос идёт в фоновом режиме
      (`background: true` + опрос статуса): один пост занимает 4-6 минут, и обычный
      держащийся сокет успевал умереть с `fetch failed` до ответа.
- [x] **Индонезийский - третий язык.** `ST_LANGS = [en, ru, id]`, роуты `/stories/id/...`,
      hreflang с `x-default`, sitemap, `llms.txt`, RSS, метка «ada di menu».
- [x] **Telegram-уведомление** (`tools/notify-telegram.mjs`) - в «SIGNA AI. Managers»
      (`-1003008104766`, топик 12613) через `@inmyrest_report_bot`. Вторым сообщением
      приходит готовый к вставке пост для Google Business Profile.
- [x] **Крон на VPS** - `0 9 * * 4` (четверг 09:00 WITA), `/home/razex/signa-cafe-repo`.
      Один запуск: анонсирует пост, вышедший сегодня, и пишет пост на следующий четверг.
      Конвейер всегда на неделю вперёд - есть время прочитать и поправить.
- [x] **JPEG-двойники для соцсетей** (`tools/og-images.mjs`) - все обложки были WebP,
      а Telegram Bot API его не принимает вовсе («failed to get HTTP URL content»),
      и часть скраперов пропускает. Теперь `og:image` ведёт на 1200x630 JPEG.
      Попутно: у `about.html` og:image вообще указывал на несуществующий файл.
- [x] **Расширена микроразметка Restaurant** - `areaServed` (Нуса Дуа, Кампьял, Букит,
      Унгасан, Беноа, Джимбаран), `amenityFeature` (WiFi, детское меню, стульчики,
      парковка), `knowsLanguage`, `keywords`, ссылка на блог. Плюс Twitter-карточки
      и `og:url` на menu/about/visit, где их не было.

- [x] **Автогенерация постов через gpt-5.5** — `tools/story-gen.mjs` + `tools/story-prompt.mjs`.
      Два прохода: писатель (EN+RU по JSON-схеме) и отдельный редактор русского языка,
      который переписывает текст как изначально русский. Валидатор ловит кальки,
      анафору, английские слова в русской прозе, «93k» вместо «93 000 IDR», длинное тире.
      Реестр блюд не даёт повторов. Сгенерировано 5 постов, 03.09 и назад по четвергам.
- [x] **Мобильная навигация** — бургер стал настоящим меню со всеми 5 разделами.
      Попутно найден и исправлен пред­существующий баг: шапка переполнялась
      (scrollWidth 410 при вьюпорте 375) и бургер уезжал за правый край - на телефоне
      навигации не было вообще.
- [x] **RSS-видимость** — `<link rel=alternate>` на всех 4 страницах + видимая кнопка
      подписки в архиве историй.
- [x] **IndexNow** — `tools/deploy-stories.sh` пингует Bing/Yandex при публикации,
      ключ `ec5bcb2216acef66611f2f3c568e3fc4.txt` в корне сайта.
- [x] **Menu JSON-LD** — было 5 пустых секций, стало 12 блюд с ценами, фото,
      `suitableForDiet` и ссылками на посты (`tools/build-menu-schema.mjs`).
- [x] **speakable** в BlogPosting — для голосовых ассистентов.

- [x] **Раздел Stories — еженедельные SEO-посты о блюдах** — server-rendered PHP
      (`story.php` / `stories.php`), EN+RU с hreflang, JSON-LD (BlogPosting + MenuItem +
      FAQPage + BreadcrumbList), RSS `/feed.xml`, генерируемый `/sitemap.xml`, `/llms.txt`
      для ИИ-ботов, вкладка Stories в админке, `tools/new-story.mjs` + `--check` валидатор.
      Два первых поста: сырники и пицца Маргарита. Задеплоено 2026-09-04, все URL 200.
- [x] **Фикс невидимости сайта для ИИ-ботов** — `<noscript>` на index/menu/about/visit
      расширены с 287 символов до 1.3-3.9 тыс. (генерируется `tools/build-noscript.mjs`
      из content.json, чтобы цены не разъезжались). `robots.txt` явно разрешает
      GPTBot / ClaudeBot / PerplexityBot / Google-Extended / CCBot по именам.
- [x] Docs: AGENTS.md и CLAUDE.md — один файл через симлинк — `5dcde82`
- [x] Analytics: tracker в site.jsx на всех 4 страницах — `bfc2f7a`
- [x] hydrateContent: version-gated localStorage — `bd4980c`
